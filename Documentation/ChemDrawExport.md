# External ChemDraw format extension

The CDX/CDXML codec implementation is no longer distributed by this package.
Applications must supply an external format extension. Legacy API entry points
remain solely for source compatibility and throw an unsupported-format error;
CDX/CDXML are excluded from the supported export catalog.

Previously published tags are immutable and retain their historical contents.
